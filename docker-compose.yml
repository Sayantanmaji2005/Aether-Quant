
services:
  aetherquant:
    build: .
    image: aetherquant:latest
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    entrypoint: ["python", "-m", "uvicorn", "aetherquant.web.app:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
