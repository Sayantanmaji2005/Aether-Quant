
services:
  aetherquant:
    build: .
    image: aetherquant:latest
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    entrypoint: ["aetherquant-web"]
    ports:
      - "8000:8000"
